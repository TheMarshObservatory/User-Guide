\documentclass[12pt,a4paper]{article}

\usepackage[pdftex]{color} 
\usepackage[pdftex]{geometry}
\geometry{hscale=0.78,vscale=0.8}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{parskip}
\usepackage{xcolor}
\usepackage{rotating}

\definecolor{linkColour}{rgb}{0,0.3,0}
\definecolor{urlColour}{rgb}{0,0.3,0}
\definecolor{citeColour}{rgb}{0,0.3,0}

\hypersetup{
  pdftitle={Windmill Hill Observing Guide},
  pdfauthor={Paul Chote, Warwick},
%  pdfpagemode=none,
  pdfborder={0 0 0},
  linkbordercolor={0.5 0.57 0.72},
  colorlinks=true,
  linkcolor=linkColour,
  urlcolor=urlColour,
  citecolor=citeColour
}


\colorlet{punct}{red!60!black}
\definecolor{background}{HTML}{EEEEEE}
\definecolor{delim}{RGB}{20,105,176}

\newcommand\digitstyle{\color{magenta!60!black}}
\makeatletter
\newcommand{\ProcessDigit}[1]
{%
  \ifnum\lst@mode=\lst@Pmode\relax%
   {\digitstyle #1}%
  \else
    #1%
  \fi
}
\makeatother

\lstdefinelanguage{json}{
    basicstyle=\scriptsize\ttfamily,
    showstringspaces=false,
    breaklines=true,
    frame=lines,
    backgroundcolor=\color{background},
    literate=
    {0}{{{\ProcessDigit{0}}}}1
    {1}{{{\ProcessDigit{1}}}}1
    {2}{{{\ProcessDigit{2}}}}1
    {3}{{{\ProcessDigit{3}}}}1
    {4}{{{\ProcessDigit{4}}}}1
    {5}{{{\ProcessDigit{5}}}}1
    {6}{{{\ProcessDigit{6}}}}1
    {7}{{{\ProcessDigit{7}}}}1
    {8}{{{\ProcessDigit{8}}}}1
    {9}{{{\ProcessDigit{9}}}}1
    {:}{{{\color{punct}{:}}}}{1}
    {,}{{{\color{punct}{,}}}}{1}
    {\{}{{{\color{delim}{\{}}}}{1}
    {\}}{{{\color{delim}{\}}}}}{1}
    {[}{{{\color{delim}{[}}}}{1}
    {]}{{{\color{delim}{]}}}}{1},
    morestring=[b]",
    morestring=[b]',
    morecomment=[l]//,
}

\lstset{columns=fixed, basicstyle=\ttfamily, basewidth=0.5em}

\definecolor{highlightColour}{rgb}{0.7,0,0}

\newcommand{\high}[1]{\textcolor{highlightColour}{#1}}


\pagestyle{fancy}
\begin{document}

\section{Introduction}

The telescope and observatory consist of several sub-systems that work together to deliver images of the sky. They are displayed graphically in Figure \ref{fig:subsystems}, and described in detail below.

\begin{figure}
    \centering
    %\includegraphics[width=5cm]{figures/subsystems.svg}
    \caption{Schematic overview of the Windmill Hill Observatory systems. (1) power control, (2) telescope mount, (3) focuser, (4) filter wheel, (5) camera, (6) dome, (7) environment monitoring}
    \label{fig:subsystems}
\end{figure}

\subsection{Power Control}

The equipment cabinet mounted on the back of the pier contains (among other things) a network-controllable power board. This is used to remotely power on and off the telescope and instrument components through computer control. This is done using the \texttt{power} command, which will be explained in Section \ref{sec:startup}.

\subsection{Telescope Mount}

The telescope mount is the fork structure that holds the Optical Tube Assembly (OTA) - the actual telescope part of the telescope - and provides computer control to point the telescope at the desired target and to automatically track the sidereal motion of the stars. It can be controlled using the \texttt{tel} command.

\subsection{Focuser}

The focuser is mounted on the back of the OTA, and provides very fine computer control to move the camera in and out to precisely align the camera detector with the image plane of the telescope optics. This alignment changes due to the thermal expansion and contraction of the metal telescope tube, which changes the separation of the primary and secondary mirrors when the temperature changes. The effect is small in absolute terms (fractions of a millimetre), but has a large impact on the focus of the camera.

The focuser is controlled using the \texttt{focus} command.

\subsection{Filter Wheel}

Most astronomical cameras are monochrome (``black and white'') because they are generally more sensitive and higher resolution than equivalent colour cameras.

Colour images are instead created by placing a piece of coloured glass (called a ``filter'') in front of the camera sensor. Single images can be taken each with a different filters, and combined into a single colour image using software.

The \emph{filter wheel} device sits between the focuser and camera, and contains several different filters that can be selected under computer control. It is controlled using the \texttt{filter} command.

\subsection{Camera}

The core component of a camera is the sensor that captures light and converts millions of individual brightness measurements into a two-dimensional image.

Traditionally, astronomical cameras generally used sensors based on the Charge Coupled Device (CCD) technology. Until recently, these offered much higher sensitivity and stability than the CMOS sensors used in consumer electronics, but were significantly (factors of $10^3 - 10^6$) more expensive.

By $\sim$2015, advancements in the CMOS technology meant that they were becoming more and more suitable for astronomical use, and had several advantages that made them \emph{more} desirable than CCDs for particular types of observation.

The camera at Windmill Hill contains a 60MP CMOS sensor that is cooled to 0$^\circ$C to reduce the impact of thermal noise (called \emph{dark current}) on the images. It contains a very accurate clock, synchronised with the GPS satellite network, to measure the starting and ending times of each image to an accuracy of $\sim$1 millisecond.

The camera is controlled using the \texttt{cam} command. Images are then processed by the \emph{data pipeline} before they are saved to disk. The data pipeline, controlled with the \texttt{pipeline} command, adds various information to the image headers and can perform various analysis (including measuring focus and \emph{world coordinate system} calibration) before archiving images to disk. Beware that \textbf{images are discarded unless archiving is enabled in the pipeline}.


The dome protects the telescope from the sun and weather during the day, and shelters it from the wind (which can cause image shaking) when observing. It contains a shutter that can be opened through computer control, and automatically rotates to allow the telescope to observe through the opening.

It takes 2-3 minutes for the shutter to fully open or close, so be mindful of the weather before you consider opening. If it starts raining, things will get wet before it closes!

\subsection{Environment Monitoring}

A weather mast on the terrace contains a collection of sensors to measure the temperature, humidity, wind speed, cloud coverage, and whether or not it is raining. These measurements allow the dome, when operated robotically (see Section \ref{sec:roboticobserving}), to automatically close when conditions become unsafe.

\section{Operating the telescope}

The Windmill Hill telescope is 100\% computer controlled - there is no eye piece, nor is it necessary to manually rotate or open the dome.

The system is operated using a web browser, with the web dashboard (see Figure \ref{fig:dashboard}) providing an overview of the current status of all systems, and a remote desktop window into the telescope control computer (TCS).

\begin{figure}
    \centering
    \includegraphics[width=16cm]{figures/dashboard.jpg}
    \caption{The web dashboard interface allows a user to monitor the observatory status and command the telescope.}
    \label{fig:dashboard}
\end{figure}

Log into the control room computer using your ITS account, and open a \emph{Google Chrome} web browser window. Enter the address \texttt{http://172.19.0.160/} to access the dashboard.

It is recommended to use two full screen windows, with one monitor displaying the \emph{Status} page and the other the \emph{Control} page.

The Control page will ask for a password to connect to the TCS; enter \textbf{obs@who} to continue. If presented with a login interface, select the \textbf{observer} user and enter the same password again.

The TCS runs the Rocky Linux operating system, with the Gnome desktop environment. Commands to operate the observatory are entered in a terminal window; if a terminal is not already present on the desktop, press the \textbf{Activities} button at the top-left of the screen, and then the \textbf{Terminal} icon in the dock at the bottom of the screen.

\subsection{Starting Up}\label{sec:startup}

Start by powering on and initialising the focuser, filter wheel, and camera. Type the following commands into the terminal window (one line at a time, pressing enter after each):
\begin{lstlisting}[language=sh]
  power switch focuser on
  focuser init
  power switch filter on
  filter init
  power switch cam on
  cam init  
\end{lstlisting}

After a few seconds, the status of the three devices on the web dashboard should change from \texttt{\color{red}POWER OFF} to \texttt{IDLE}. The camera will automatically start cooling the sensor to $0^\circ$C, which can also be monitored on the dashboard.

Next, power on and initialise the telescope:
\begin{lstlisting}[language=sh]
  power switch tel on
  tel init
\end{lstlisting}

The \texttt{init} command will take 1-2 minutes to complete, during which time you will not be able to enter commands in the terminal. You will know it has completed once control returns on the terminal.

Now, go into the dome and check that the telescope is aligned with the zero markers, and shown in Figure \ref{fig:telescopemarkers}. If it is not, proceed to Appendix \ref{app:zerotelescope} before continuing. This will be necessary if the telescope was powered off without first (or too quickly after) issuing the \texttt{tel kill} command (see Section \ref{sec:shutdown}).

\begin{figure}
    \centering
    %\includegraphics[width=16cm]{figures/telescopemarkers.jpg}
    \caption{The telescope should be aligned with the zero markers after initialising. The telescope should be re-zeroed if the hour-angle axis is offset by more than a degree or two.}
    \label{fig:telescopemarkers}
\end{figure}

The dome must be powered on manually, using the switch on the wall of the telescope room (see Figure \ref{fig:domepower}. Turn it clockwise to the vertical position to enable power, then return to the control computer.

\begin{figure}
    \centering
    \includegraphics[width=5cm]{figures/domepower.jpg}
    \caption{The switch in the telescope room to turn on the Dome. It is shown in the off position.}
    \label{fig:domepower}
\end{figure}

The dome must be \emph{homed} before it can be used; this procedure calibrates the rotation so it can accurately track the telescope. Issue the following commands to initialise and home the dome:
\begin{lstlisting}[language=sh]
  dome init
  dome home
\end{lstlisting}

Homing will take a couple of minutes, and will return control to the terminal once it has completed.

The system is now ready for observing.

\subsection{Manual Observing}
TODO: Expand wording

e.g. to observe Messier Object 101, the Pinwheel Galaxy:

\begin{lstlisting}[language=sh]
  tel track 14:03:12.6 54:20:57
  cam exposure 10
  filter set V
  pipeline type SCIENCE
  pipeline object M101
  pipeline preview
  pipeline archive enable
  cam start 5
\end{lstlisting}

Wait for camera status to be \texttt{IDLE}
\begin{lstlisting}[language=sh]
  filter set R
  cam start 5
\end{lstlisting}

Repeat for other filters...

Manual offsetting...
\begin{lstlisting}[language=sh]
  
  pipeline wcs enable
  tel offset 0:0:30 0:5:0
  cam start 1
  tel offset ...
  pipeline wcs disable
\end{lstlisting}

WCS calculations may fail if the image contains a complicated object, such as a large galaxy or cluster. In these cases, use the \emph{blind offset} technique. Command the telescope to a nearby position (e.g. 1 degree north) that is more amenable for the WCS solutions, and then once the telescope is precisely aligned with that field offset back (e.g. 1 degree south) to the target field. The telescope can make very accurate relative movements over a small separation, even if the overall abosolute positioning is poor.

Manual focusing...

\begin{lstlisting}[language=sh]
  pipeline hfd enable
  focus offset 2000
  cam start 1
  focus offset 500
  cam start 1
  ...
  pipeline hfd disable
\end{lstlisting}

\textbf{DO NOT USE \texttt{dome open} to open the dome manually!}

TODO: Add an \texttt{ops dome open} command to opsd.


Manual dark frames/biases...

\begin{lstlisting}[language=sh]
  filter set block
  cam exposure 0
  pipeline type BIAS
  pipeline prefix bias
  pipeline archive enable
  cam start 21
\end{lstlisting}
\begin{lstlisting}[language=sh]
  cam exposure 10
  pipeline type DARK
  pipeline prefix dark-10s
  cam start 21
\end{lstlisting}

\subsection{Robotic Observing}\label{sec:roboticobserving}
TODO: Expand wording

\begin{lstlisting}[language=sh]
  ops validate myobservingplan.json
  ops tel auto
  ops dome auto
  ops schedule myobservingplan.json
\end{lstlisting}

Figure \ref{fig:exampleplan} demonstrates an example observing plan that automatically opens the dome at sunset, acquires sky flat calibration frames, focuses the telescope, and then acquires a sequence of images. The dome will automatically close at midnight (or if the weather becomes unsafe), and the camera will automatically warm and power off once observations are complete.

\begin{figure}
    \centering
    \lstinputlisting[language=json]{figures/example_plan.json}
    \caption{An example robotic observing plan.}
    \label{fig:exampleplan}
\end{figure}


\subsection{Shutting down}\label{sec:shutdown}
TODO: Expand wording and figures around the following procedure:

\begin{lstlisting}[language=sh]
  cam temperature warm
\end{lstlisting}

Wait for status to report \texttt{\color{red}WARM} on the dashboard.

\begin{lstlisting}[language=sh]
  cam kill
  power switch cam off
  filter kill
  power switch filter off
  focus kill
  power switch focus off
  tel park
  tel kill
  power switch tel off
\end{lstlisting}

Wait for dome to stop moving and ensure it is closed
\begin{lstlisting}[language=sh]
  dome kill
\end{lstlisting}

and power off by rotating the switch on the wall anti-clockwise into the horizontal position.

\appendix 
\newpage
\section{Re-zeroing telescope axes}\label{app:zerotelescope}

TODO: Expand wording and figures around the following procedure:
\begin{itemize}
    \item Manually move off the limit if security system was tripped
    \item Make sure telescope has been initialised
    \item Use hand panel to position telescope on zero markers
    \item \texttt{tel zero} and wait for control to return to the terminal
\end{itemize}

\newpage
\section{Technical Information}\label{app:technical}

The observatory is controlled using the \href{https://github.com/rockit-astro}{Robotic Observatory Control Kit} software platform. Table \ref{tab:rockit-modules} provides a listing of the modules used.
\begin{sidewaystable}
    \centering
    \begin{tabular}{c|c|c|c}
        Module & Purpose & Computer & \texttt{systemd} service name(s)  \\
        \hline
        \href{https://github.com/rockit-astro/ashdomed}{\texttt{ashdomed}} & Dome control & DCS & \texttt{ashdomed@warwick}\\
        \href{https://github.com/rockit-astro/atlasd}{\texttt{atlasd}} & Focus control & TCS & \texttt{focusd@warwick}\\
        \href{https://github.com/rockit-astro/camd-qhy}{\texttt{camd-qhy}}, \href{https://github.com/rockit-astro/camd-client}{\texttt{camd-client}} & Camera control & TCS & \texttt{qhy\_camd@warwick}\\
        \href{https://github.com/rockit-astro/cfwd}{\texttt{cfwd}} & Filter wheel control & TCS & \texttt{cfwd@warwick}\\
        \href{https://github.com/rockit-astro/cloudwatcherd}{\texttt{cloudwatcherd}} & Sky temperature & TCS & \texttt{cloudwatcherd@warwick}\\
        \href{https://github.com/rockit-astro/diskspaced}{\texttt{diskspaced}} & Disk space monitor & TCS & \texttt{diskspaced@warwick}\\
        \href{https://github.com/rockit-astro/environmentd}{\texttt{environmentd}} & Environment aggregation & TCS & \texttt{environmentd@warwick}\\
        \href{https://github.com/rockit-astro/ephemd}{\texttt{ephemd}} & Sun/moon information & TCS & \texttt{ephemd@warwick}\\
        \href{https://github.com/rockit-astro/meaded}{\texttt{meaded}} & Telescope control & TCS & \texttt{meaded@warwick}\\
        \href{https://github.com/rockit-astro/obslogd}{\texttt{obslogd}} & Dashboard log database & TCS & \texttt{obslogd@warwick}\\
        \href{https://github.com/rockit-astro/opsd}{\texttt{opsd}} & Robotic operations control & TCS & \texttt{opsd@warwick}\\
        \href{https://github.com/rockit-astro/pipelined}{\texttt{pipelined}} & Camera data pipeline & TCS & \texttt{pipelined@warwick}, \texttt{pipeline\_workerd@warwick}\\
        \href{https://github.com/rockit-astro/powerd}{\texttt{powerd}} & Power control & TCS & \texttt{powerd@warwick}\\
        \href{https://github.com/rockit-astro/rhusbd}{\texttt{rhusbd}} & Dome temp/humidity & DCS & \texttt{rhusbd@warwick}\\
        \href{https://github.com/rockit-astro/vaisalad}{\texttt{vaisalad}} & Ext. weather & TCS & \texttt{vaisalad@warwick}\\
        \href{https://github.com/rockit-astro/weatherlog}{\texttt{weatherlog}} & Dashboard weather database & TCS & \texttt{update-weather-database}\\
        \href{https://github.com/rockit-astro/dashboard-warwick}{\texttt{dashboard-warwick}} & Web dashboard & TCS & \texttt{dashboard}\\
    \end{tabular}
    \caption{List of rockit modules used at Windmill Hill Observatory.}
    \label{tab:rockit-modules}
\end{sidewaystable}

The remote-desktop \textbf{observer} user does not have admin permissions, so maintenance and debug tasks should be undertaken by logging into a physical session on the TCS computer as the \textbf{ops} user. The two accounts can be quickly distinguished by the desktop background colour -- the observer user is purple, and the ops user is grey. The password for the \textbf{ops} user is listed on the \href{https://github.com/WarwickWindmillHill/docs/wiki/Network-configuration}{Network Configuration wiki page}.

TODO: software updates, systemd commands

\end{document}